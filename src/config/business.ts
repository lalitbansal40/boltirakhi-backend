/**
 * Business details that appear in customer-facing messages.
 *
 * The storefront has its own copy in lib/business.ts — the two run in
 * different processes and cannot import from each other. If this number
 * changes, both change.
 */
export const SUPPORT_PHONE = '+91 96641 14023';
